#import "@preview/bubble-sysu:0.1.0": *

#show: report.with(
  title: "大作业：多路超图划分算法",
  subtitle: "VLSI 设计导论大作业",
  student: (name: "元朗曦", id: "23336294"),
  school: "计算机学院",
  major: "计算机科学与技术",
  class: "计八",
)

#outline()

= 摘要

本大作业选择课程进阶要求中的“划分”方向，在实验一二路超图划分的基础上实现多路划分算法。程序读取 ISPD `.hgr` 网表，将顶点划分到 $K$ 个不同部分中，并以跨越多个部分的超边数量作为割代价。在满足每个划分规模下界和近似均分容量的条件下，算法通过确定性的初始构造和局部改进降低割代价。

实现上，程序不再沿用实验一中按指针对象组织图结构的写法，而是采用 `vector<vector<int>>` 保存超边和顶点关联关系，从而降低读取和遍历时的额外开销。算法支持命令行设置划分数量 $K$、均衡下界 $r$ 和局部搜索轮数。实验脚本能够批量运行 benchmark 并生成结果表格，便于复现实验和论文分析。

= 引言

电路网表划分是 VLSI 设计流程中的基础问题之一。随着芯片规模和互连复杂度提高，单纯的二路划分难以覆盖多 FPGA 系统、多芯粒系统或多区域布局中的资源分配需求。因此，多路超图划分成为更贴近实际设计场景的问题。与普通图相比，超图能够自然表示一条线网同时连接多个元件的情况，因此更适合用于网表建模。

课程进阶要求提出了多路划分和均衡约束。本实验关注其中的多路划分部分：给定一个超图 $H = (V, E)$ 和划分数 $K$，需要将所有顶点分配到 $K$ 个部分中，使跨分区超边数量尽可能小，同时尽量保证各部分规模平衡。该问题通常具有较高组合复杂度，实际系统常使用多层次划分、局部搜索或并行优化等策略 @gottesbueren2022parallel @gottesbueren2023scalable。

本实验的目标不是复现完整工业级划分器，而是在课程代码基础上实现一个可解释、可复现、可扩展的多路划分 baseline。算法全程不使用随机数，因此固定输入和参数下输出稳定，便于对比不同参数对割代价和均衡性的影响。

= 方法

== 问题定义

输入网表被建模为超图 $H = (V, E)$。其中 $V$ 是顶点集合，表示电路元件；$E$ 是超边集合，表示线网。一条超边可以连接多个顶点。

给定划分数量 $K$，算法输出函数：

$ p: V -> {0, 1, ..., K - 1} $

如果一条超边中的顶点被分配到至少两个不同部分，则该超边贡献 1 的割代价。总割代价定义为：

$ "cut"(p) = sum_(e in E) I(abs({p(v) | v in e}) >= 2) $

均衡约束由两个部分组成。第一，每个划分的规模不小于用户设置的比例 $r$：

$ abs(V_i) >= floor(r dot abs(V)) $

第二，程序采用接近均分的容量上界：

$ abs(V_i) <= ceil(abs(V) / K) $

当顶点总数不能被 $K$ 整除时，允许不同划分之间的顶点数量相差 1。

== 数据结构

实验一代码使用 `Graph`、`Node` 和 `Net` 类保存网表，但节点创建过程中包含线性查找。在大规模 benchmark 上，这会带来明显额外开销。本大作业重新实现轻量数据结构：

```cpp
struct Hypergraph {
    int node_count = 0;
    vector<vector<int>> nets;
    vector<vector<int>> incident_nets;
};
```

`nets[i]` 保存第 `i` 条超边连接的顶点；`incident_nets[v]` 保存顶点 `v` 所连接的超边编号。顶点在内部使用 0-based 编号，但输出文件仍按原始 1-based 顶点顺序逐行写出划分编号。

== 初始划分

初始阶段先按顶点度数从高到低排序；如果度数相同，则按顶点编号从小到大排序。随后依次放置顶点。对于每个顶点，算法尝试所有尚未达到容量上界的划分，选择新增割代价最小的划分。若新增割代价相同，则优先选择当前规模更小的划分；如果仍相同，则选择编号更小的划分。

这种策略兼顾了两个目标：高度数顶点更早决策，有助于减少关键连接被随意切开；平手时优先补小划分，有助于保持规模均衡。

== 局部改进

局部改进阶段进行固定顺序扫描。对每个顶点，算法尝试将其移动到其他划分，并计算与该顶点相关超边的割状态变化。只有当移动满足规模下界和容量上界，且割代价严格下降时，才接受移动。

算法最多执行用户设置的 `passes` 轮。如果某一轮没有任何移动被接受，则提前结束。由于扫描顺序、平手规则和参数都固定，算法具有确定性。

= 实验

== 运行方式

在 `lab/final` 目录下编译：

```bash
make clean
make all
```

运行单个 benchmark：

```bash
make run CASE=data/ibm01.hgr PARTS=4 R=0.0 PASSES=8
```

批量运行：

```bash
bash run.sh
```

脚本会读取 `data/*.hgr`，并将结果写入 `report/result.csv`。

== 实验结果

结果表中的 `cut` 表示多路割代价，`imbalance` 表示最大划分规模与最小划分规模之差。若尚未运行脚本，表格会只显示 CSV 表头；运行 `bash run.sh` 后重新编译报告即可得到完整结果。

#let result-data = csv("./result.csv")
#let result-header = result-data.first().map(it => strong(text(size: 8pt, it)))
#let result-rows = result-data.slice(1).flatten()

#figure(
  table(
    columns: 8,
    align: center,
    inset: 2pt,
    table.header(..result-header),
    ..result-rows,
  ),
  caption: [多路划分实验结果],
)

== 分析

与实验一的二路划分相比，本实现支持任意 $K >= 2$ 的多路输出，并提供均衡下界参数 $r$。当 $K = 4$ 且 $r = 0.25$ 时，各划分规模被压到接近完全均分；当 $r = 0$ 时，初始化阶段仍使用容量上界保持近似均衡，因此不会出现所有顶点被放入同一划分的退化情况。

本方法的主要优点是简单、稳定、易复现。它不依赖随机初始解，也不需要外部划分库。主要不足是局部搜索只接受单点移动，容易陷入局部最优；同时它没有实现多层次粗化与细化，因此在大规模超图上与成熟划分器仍会有差距。后续可以参考深层多级划分和共享内存高质量超图划分方法进一步增强结果质量 @sanders2023distributed @gottesbueren2023scalable。

= 结论

本大作业完成了一个独立的多路超图划分程序。程序能够读取课程 `.hgr` benchmark，按照用户指定的划分数量和均衡下界生成划分结果，并输出割代价、规模不均衡度和运行时间。工程中提供了 Makefile、批量运行脚本、README 和 Typst 报告源文件，满足代码可执行、结果可复现和报告可继续扩展的要求。

当前实现覆盖课程进阶要求中的多路划分部分，未覆盖拓扑约束、固定约束和并行化。若继续扩展，可以优先加入多层次粗化、FM 风格增益桶，以及拓扑图约束检查，从而进一步贴近 TopoPart 类方法的目标。

= 参考文献

#bibliography("refs.bib", style: "ieee", title: none)
